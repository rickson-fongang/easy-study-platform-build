/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},    // ✅ USE OLD SYNTAX
    autoprefixer: {},   // ✅ ADD AUTOPREFIXER FOR COMPATIBILITY
  },
}

export default config
